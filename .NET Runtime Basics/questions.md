NET Architecture & Runtime

Explain the complete execution flow of a .NET application
From source code → IL → JIT → native code.
When does JIT occur? What role does the CLR play?

What responsibilities does the CLR have at runtime?
Explain memory management, exception handling, security, type safety, and thread management.

How does .NET ensure language interoperability?
Explain CTS and CLS with real examples.

What happens internally when an EXE is executed in .NET?

How does the runtime locate and load assemblies?
